// If else statements

var firstName = 'John';
var civilStatus = 'married';

if (civilStatus === 'married') {
    console.log(firstName + ' is married!');
} else {
    console.log(firstName + ' will hopefully marry soon :)');
}

var isMarried = false;
//isMarried === true is not needed as isMarried is already a boolean value and is true by default
if (isMarried === 'married') {
    console.log(firstName + ' is married!');
} else {
    console.log(firstName + ' will hopefully marry soon :)');
}
